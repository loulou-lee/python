# 자원의 재활용 : 클래스는 다른 클래스를 불러다 사용 가능
# 클래스의 포함관계(has a)

class Machine: #  어떤 클래스에서든 호출될 수 있다.
    cupCount = 1 # 몇잔
    '''
    def LeftTurn(self, quantity):
        self.quantity = quantity
        return '좌회전'
    '''
    def showData(self):
        cupCount = input('몇 잔을 원하세요 : ')
        return 0


class CoinIn:
    coin = input('동전을 입력하세요 : ')
    change = ''
    
    def __init__(self): #생성자
        self.handle = Machine() # 클래스의 포함
        
    def culc(self, cupCount):
        if cupCount > 0:
            cupCount = self.handle.cupCount
            self.change = self.coin - 200*cupCount
            #if self.change < 0:
                #print('')
        elif cupCount <= 0:
            print('다시 입력하세요')
            
if __name__ == '__main__':
    order = CoinIn()
    print(order.handle.showData() + '커피' +  + '잔과 잔돈' + '원')
    #tom.TurnHandle(10)
    #print(tom.ownerName + '의 회전량은 ' + tom.turnShowMessage + str(tom.handle.quantity))
 
    